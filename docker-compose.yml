version: '3.8'
services:
  bank:
    build: .
    container_name: concurrent_bank
    volumes:
      - ./accounts.txt:/app/accounts.txt
      - ./logs:/app/logs
    environment:
      - N_BRANCHES=3
      - M_THREADS=5
      - N_TX=20